import { Link } from "react-router-dom";

function Navbar() {
  return (
    <header>
        <section>
            <Link to="/">forbidden fruit</Link>


        </section>
    </header>
  )
}

export default Navbar;
